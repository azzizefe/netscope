# Netscope %100 Tamamlanma Yol Haritası (Senior Technical Backlog)

Bu doküman, Netscope projesinin kalan eksiklerini sıfır hata ile tamamlayarak ürünü %100 olgunluğa ulaştırmak ve açık kaynak topluluğuna/kurumsal müşterilere güvenle sunmak için atılması gereken teknik adımları, mimari tasarımları ve komutları detaylandırmaktadır.

---

## 1. Adım: Git Depo Geçmişinin Arındırılması (Güvenlik & Lisans)
Npcap SDK ikililerinin (`Packet.lib`, `wpcap.lib`) ticari lisans gereksinimleri nedeniyle deponun Git geçmişinde bulunması yasal risk oluşturur. Ayrıca geçmişteki build çıktıları (.exe, .zip, .msi) `.git/` boyutunu şişirmektedir.

- [ ] **1.1. Deponun yedeğini alın (Yıkıcı işlem öncesi önlem):**
  ```bash
  git clone --mirror . ../netscope-history-backup.git
  ```
- [ ] **1.2. filter-repo ile lisanssız ve büyük dosyaları tamamen geçmişten silin:**
  ```bash
  git filter-repo --invert-paths \
    --path npcap-sdk \
    --path dist/windows/netscope.exe \
    --path dist/windows/netscope_0.1.0_x64-setup.exe \
    --path dist/windows/netscope_0.1.0_x64_en-US.msi \
    --path dist/netscope-windows-v0.1.0-x64.zip
  ```
- [ ] **1.3. Uzak depoyu (remote) tekrar bağlayın ve force-push yapın:**
  ```bash
  git remote add origin https://github.com/azzizefe/netscope.git
  git push --force --all origin
  git push --force --tags origin
  ```
- [ ] **1.4. Doğrulama:** Aşağıdaki komutların çıktıları boş dönmelidir:
  ```bash
  git log --all --oneline -- npcap-sdk
  git rev-list --objects --all | grep -iE "\.(exe|msi|zip)$"
  ```

---

## 2. Adım: Çoklu Platform Güvenlik Duvarı Bloklama (OS Integration) — ✅ TAMAMLANDI (2026-08-02)
IP engelleme özelliği şu anda yalnızca Windows üzerinde `netsh` komutlarıyla çalışmaktadır. Linux ve macOS sistemler için gerçek güvenlik duvarı backend'leri yazılmalıdır.

> **Durum:** `crates/core/src/firewall.rs` artık üç platformu da kapsıyor ve
> `is_supported()` üçünde de `true` dönüyor (`support_flag_matches_platform`
> testi bunu sabitliyor). **Tek sapma:** Linux tarafı `nftables` yerine
> `iptables`/`ip6tables` kullanıyor. Modern dağıtımlarda `iptables` zaten
> nft-destekli bir uyumluluk katmanıdır, ama yalnızca `nft` kurulu bir hostta
> engelleme başarısız olur. Gerçek bir `nft` backend'i isteniyorsa ayrı bir iş
> kalemi olarak açılmalı.

- [x] **2.1. Linux backend'ini yazın:** *(iptables/ip6tables olarak yapıldı)*
  [firewall.rs](file:///c:/Users/efe/Desktop/netscope/crates/core/src/firewall.rs) içerisindeki `cfg(not(windows))` modülüne `nftables` desteği ekleyin. Linux üzerinde root yetkileriyle şu kurallar eklenmeli ve silinmelidir:
  ```bash
  # İlk başlatmada tablo ve zincir oluşturma:
  nft add table inet netscope
  nft add chain inet netscope filter { type filter hook input priority filter \; policy accept \; }
  
  # Kural ekleme (Block):
  nft add rule inet netscope filter ip saddr <ip> drop
  
  # Kural silme (Unblock):
  # Kuralın handle ID'sini bulup silmek veya tabloyu tamamen temizlemek:
  nft flush table inet netscope
  ```
- [x] **2.2. macOS `pfctl` (Packet Filter) Backend'ini yazın:**
  [firewall.rs](file:///c:/Users/efe/Desktop/netscope/crates/core/src/firewall.rs) içerisine macOS pfctl anchor'larını manipüle edecek yapıyı kurun:
  ```bash
  # Kural ekleme:
  echo "block drop from <ip> to any" | pfctl -a netscope -f -
  
  # Aktifleştirme:
  pfctl -e
  ```
- [x] **2.3. Platform destek flag'lerini güncelleyin:**
  [firewall.rs](file:///c:/Users/efe/Desktop/netscope/crates/core/src/firewall.rs) altındaki `is_supported()` fonksiyonunu Windows, Linux ve macOS için `true` dönecek şekilde güncelleyin.
- [x] **2.4. Test edin:**
  ```bash
  cargo test -p netscope-core --lib support_flag_matches_platform
  ```

---

## 3. Adım: macOS Dağıtım & Notarization Altyapısı — ✅ AMACI KARŞILANDI (2026-08-03'te doğrulandı)
macOS kurulum paketinin (DMG/App) Gatekeeper uyarıları vermeden çalışması ve Intel/Apple Silicon cihazların ikisinde de desteklenmesi gerekmektedir.

> **Durum:** Bu adımın hedefi — her iki mimarinin desteklenmesi ve Gatekeeper'ın
> engellememesi — [release.yml](.github/workflows/release.yml)'de zaten
> karşılanıyordu; bu doküman Adım 2 ve 5 gibi geride kalmıştı.
>
> **3.1 — yöntem farkı, eksik değil.** Tek bir `lipo` universal binary yerine
> matris **iki ayrı binary** üretiyor: `aarch64-apple-darwin` ve
> `x86_64-apple-darwin`. Intel Mac'ler 2026-07-30'a kadar hiç yoktu, o gün
> eklendi. İndirme başına daha küçük bir dosya demek olduğu için bu tercih
> bilinçli; `lipo` ile birleştirilmiş üçüncü bir artefakt isteniyorsa ayrı bir
> iş kalemi olarak açılmalı.
>
> **3.2 — placeholder değil, gerçek.** `codesign --options runtime --timestamp`
> ardından `xcrun notarytool submit --wait` çalışıyor; anahtarlar GitHub
> secret'larından geliyor (`APPLE_CERTIFICATE`, `APPLE_SIGNING_IDENTITY`,
> `APPLE_ID`, `APPLE_PASSWORD`, `APPLE_TEAM_ID`) ve secret yoksa iş sessizce
> geçmiyor — `::warning::` ile "imzasız yayınlanıyor, Gatekeeper engelleyecek"
> diyor. Çıplak bir CLI binary'si staple edilemediği için `spctl` ile çevrimiçi
> doğrulama yapılıyor.
>
> **Kalan tek şey senin elinde:** yukarıdaki beş secret'ın depoya girilmesi.
> Girilmeden her macOS sürümü imzasız çıkar.

- [x] **3.1. Universal Binary Derleme Adımını ekleyin:** *(iki ayrı arch binary'si olarak yapıldı)*
  [.github/workflows/release.yml](file:///c:/Users/efe/Desktop/netscope/.github/workflows/release.yml) içerisine `universal-apple-darwin` target'ını ekleyin ve derleme sonrasında `lipo` ile binary'leri birleştirin:
  ```bash
  lipo -create -output netscope-universal target/x86_64-apple-darwin/release/netscope target/aarch64-apple-darwin/release/netscope
  ```
- [x] **3.2. Apple Notarization (Noter Onayı) Entegrasyonu:**
  [.github/workflows/release.yml](file:///c:/Users/efe/Desktop/netscope/.github/workflows/release.yml)'deki placeholder notarization adımlarını gerçek API çağrılarıyla güncelleyin:
  ```bash
  xcrun notarytool submit dist/netscope.dmg --keychain-profile "netscope-profile" --wait
  ```

---

## 4. Adım: Tauri Command Otomatik Test Kapsamı — ✅ TAMAMLANDI (2026-08-03)
[lib.rs](file:///c:/Users/efe/Desktop/netscope/desktop/src-tauri/src/lib.rs) 39 Tauri komutu içeriyor ve **18'i testlerden doğrudan çağrılıyor**. Kalan 21'i Tauri runtime'ı olmadan çağrılamaz; onların yerine **karar veren mantık** çıkarılıp testlendi (aşağıya bak). Bu sayı kasıtlı olarak 18/39'da duruyor — kapsanan şey sarmalayıcı değil, mantık.

> **Uyarı — kapsam sayısını kovalamayın.** Bu adım daha önce "tüm komut yolları
> için test kapsamı tamamlandı" başlığıyla kapatılmış, eklenen testlerin bir
> kısmı ise `assert!(x.is_ok() || x.is_err())` gibi tanımı gereği geçen
> iddialardan oluşuyordu. Sayı arttı, güvence artmadı. Testsiz kalan 21 komut:
> `start_capture`, `stop_capture`, `open_pcap`, `save_pcap`, `geoip_*`,
> `escalation_*`, `notification_*`, pencere yönetimi ve `relaunch_elevated`.
> Bunların çoğu gerçek donanım/yetki istiyor — mock'lanamayanlar
> [UNTESTED.md](UNTESTED.md)'ye yazılmalı, uydurma testle kapatılmamalı.

> **2026-08-03 — yöntem kararı ve ilk uygulaması.** 21 komutun **hepsi**
> `State<'_, Mutex<…>>` ya da `AppHandle` alıyor, yani hiçbiri Tauri runtime'ı
> olmadan doğrudan çağrılamaz. Doğru yol `mock_builder()` değil: komutun
> gövdesindeki mantığı **saf bir fonksiyona** çıkarmak ve onu test etmek;
> geriye kalan sarmalayıcı bir kilit alıp delege etmekten ibaret kalıyor.
> Ajanın `verify_signature_with`'i de aynı şekilde yazılmış.
>
> İlk uygulama `save_pcap`/`save_pcap_encrypted` oldu ve **gerçek bir hata
> ortaya çıkardı**: iki komut dosya adını farklı okuyordu — biri `.pcapng`,
> diğeri `.pcapng.enc`. `session.pcapng` adıyla şifreli kaydedilen bir yakalama
> **klasik pcap baytları** içeriyordu. Şifreleme, yazma ve çözme sorunsuz
> geçiyor; yanlışlık ancak dosyayı açan araçta ortaya çıkıyordu. Ortak bir
> `wants_pcapng()` + `encode_capture()` yazıldı, ikisi de onu kullanıyor,
> `the_extension_picks_the_format_the_same_way_encrypted_or_not` sabitliyor.
>
> **Komut sayacı hâlâ 18/39 ve öyle kalmalı** — kapsanan şey mantık, sarmalayıcı
> değil. Sayıyı yükseltmek için sarmalayıcıya test yazmak, bu adımın ilk
> kapatılışındaki hatanın aynısı olur.

- [x] **4.1. Kalan `State`-tabanlı komutların mantığını saf fonksiyonlara çıkarın:**
  `get_escalation_status`'tan `escalation_off()` ve `active_escalations()`
  çıkarıldı ve teste bağlandı — ikisi de gerçek karar içeriyordu: "kapalı"nın iki
  farklı sebebi (hiç açılmamış / açık ama rota boş) ve zincirin sonunu geçmiş bir
  eskalasyonun `Top` diye listelenmesi + en eskisinin başa alınması.
  **Geri kalanlarda çıkarılacak mantık yok:** `get_app_config`,
  `get_notification_channels` ve `reload_plugins` zaten saf `config_info()` /
  `notification_channels()` fonksiyonlarına delege ediyor (ikincisi testli);
  `acknowledge_escalation`, `resolve_escalation` ve `stop_capture` bir kilit alıp
  `netscope-core`'a devrediyor — çekirdek tarafı kendi testlerine sahip.
  `geoip_lookup` ve `get_capture_stats` bir MaxMind veritabanı / çalışan bir
  yakalama motoru gerektiriyor, ikisi de birim testi değil.
  (`test_notification_channel` gerçekten ağa çıkıyor — testte çağrılmamalı.)
- [x] **4.2. `AppHandle` gerektirenleri [UNTESTED.md](UNTESTED.md)'ye yazın, test uydurmayın:**
  `start_capture`, `start_remote_capture`, `open_pcap`, `open_pcap_encrypted`,
  `open_new_window`, `open_detached_window`, `relaunch_elevated` — pencere açıyor,
  olay yayınlıyor ya da süreci yükseltilmiş yetkiyle yeniden başlatıyorlar.
- [x] **4.3. Testleri çalıştırın:**
  ```bash
  cargo test -p netscope-desktop
  ```

---

## 5. Adım: CI/CD Pipeline Yeşillendirilmesi — ✅ TAMAMLANDI (2026-08-03'te doğrulandı)
GitHub Actions üzerindeki runner'larda işletim sistemi seviyesindeki bağımlılıklar çözülerek CI derleme hataları giderilmelidir.

> **Durum:** Bu adımdaki iki iş kalemi de [ci.yml](.github/workflows/ci.yml)'de
> zaten mevcuttu — bu doküman geride kalmıştı. `libpcap-dev` hem `lint`, hem
> `test` (Linux), hem `bench` job'ında kuruluyor; `protoc` `arduino/setup-protoc`
> ile hem lint hem test job'ında; `frontend` job'ı wasm32 hedefiyle
> `netscope-wasm`'ı derleyip `wasm-bindgen-cli` ile binding üretiyor ve vitest'i
> koşuyor. `RUST_MIN_STACK` da `.cargo/config.toml`'da tanımlı, yani Windows
> runner'ında STATUS_STACK_OVERFLOW riski yok.
>
> **2026-08-03'te kapatılan gerçek boşluk:** clippy adımı `--all-targets`
> olmadan koşuyordu, yani **test kodu hiç lint edilmiyordu** — bu deponun
> doğruluk korumalarının yaşadığı yer. `#[cfg(test)]` içindeki bir lint hatası
> CI'dan geçip yerel makinede patlıyordu. Bayrak eklendi.

- [x] **5.1. Linux Runner bağımlılıklarını ekleyin:**
  [.github/workflows/ci.yml](file:///c:/Users/efe/Desktop/netscope/.github/workflows/ci.yml) dosyasına `libpcap-dev` ve `protoc` (gRPC için) kurulumlarını ekleyin:
  ```yaml
  - name: Install dependencies
    run: |
      sudo apt-get update
      sudo apt-get install -y libpcap-dev protobuf-compiler
  ```
- [x] **5.2. WebAssembly Derleme bağımlılıklarını CI'a entegre edin:**
  Frontend testlerinin koşabilmesi için CI üzerinde `wasm-pack` veya `wasm-bindgen-cli` kurulumunu yapıp WASM paketini build eden adımları ekleyin:
  ```yaml
  - name: Build WASM modules
    run: |
      cargo install wasm-bindgen-cli
      ./tools/build-wasm.sh
  ```

---

## 6. Adım: Ajan & Sunucu Güvenliği (Fleet Hardening) — ✅ TAMAMLANDI (2026-08-03)
Merkezi yönetim sunucusu (`netscope-server`) ile sensör ajanları (`netscope-agent`) arasındaki haberleşme güvenli hale getirilmelidir.

> **6.1 zaten yapılmıştı, ama testi yarımdı.** `upgrade.rs` minisign (Ed25519)
> ile fail-closed doğrulama yapıyor ve anahtar derleme zamanında gömülü
> (`NETSCOPE_AGENT_UPDATE_PUBKEY`) — diskten okunan bir anahtar, o diske
> yazabilen saldırganın kendi imzasını doğrulardı. Eksik olan şuydu: **her test
> bir reddi iddia ediyordu**, yani fonksiyon koşulsuz `Err` dönse suite yine
> yeşildi. Artık test kendi anahtar çiftini üretip imzalı bir yükün kabul
> edildiğini, aynı imzanın başka baytlar için geçmediğini doğruluyor.
>
> **6.2 gerçek bir açıktı.** gRPC sunucusu `--tls-cert/--tls-key/--tls-ca`
> bayraklarını **tamamen yok sayıyordu**: `SensorService` — kayıt, heartbeat,
> olay akışı ve sensöre komut gönderen `SendCommand` — düz metin ve istemci
> kimlik doğrulaması olmadan servis ediliyordu, üstelik yanındaki HTTP portu
> loga "TLS 1.3 enabled (mTLS: true)" yazarken. Artık iki dinleyici de aynı
> `tls_mode()` kararını kullanıyor; eksik bir yapılandırma (yalnız sertifika,
> yalnız anahtar, ya da yalnız CA) düz metne düşmek yerine hata veriyor.

- [x] **6.1. Ed25519 Güncelleme Doğrulaması:**
  [upgrade.rs](file:///c:/Users/efe/Desktop/netscope/crates/agent/src/upgrade.rs) içindeki imza kontrol mekanizmasını aktif edin. Agent binary'sine gömülü public key ile gelen yeni güncelleme paketinin imzasını doğrulamadan kurulmasını engelleyin.
- [x] **6.2. gRPC mTLS Entegrasyonu:**
  Sunucu ve Ajan gRPC bağlantılarında Tonic TLS yapılandırmasını kullanarak çift taraflı (mutual TLS) sertifika doğrulaması ekleyin:
  ```rust
  let cert = std::fs::read_to_string("client.pem")?;
  let key = std::fs::read_to_string("client.key")?;
  let identity = Identity::from_pem(cert, key);
  ```

---


